{
  "name": "portfolio-version2",
  "version": "1.0.0",
  "engines": {
    "node": "20.10.0",
    "npm": "10.2.3"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd frontend && npm install --include=dev && npx vite build && cd ../backend && npm install",
    "heroku-prebuild": "npm install -g vite"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.2",
    "vite": "^5.4.8"
  }
}
